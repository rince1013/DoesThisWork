# fly.toml app configuration file generated for doesthiswork on 2026-04-15T03:14:22Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'doesthiswork'
primary_region = 'ord'

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[mounts]]
  source = "dtw_data"
  destination = "/data"

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
